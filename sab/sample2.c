#include<bits/stdc++.h>
#include<regex.h>

int main(){
    regex_t regex;
    int re;
    int re1;
    int re2;
    int re3;
    int re4;
    char x[40];

    re = regcomp(&regex,"=",0)
    re = regexec(&regex,x,0,NULL,0)
    if (re == 0){
        print
    } 
}